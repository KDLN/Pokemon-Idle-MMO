---
phase: 15-theme-exploration
plan: 03
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - apps/web/src/stories/showcase/BattleUI.mdx
  - apps/web/src/stories/showcase/PartyUI.mdx
  - apps/web/src/stories/showcase/MapUI.mdx
  - apps/web/src/stories/showcase/SocialUI.mdx
  - apps/web/src/stories/showcase/InventoryUI.mdx
  - apps/web/src/stories/showcase/CoreUI.mdx
autonomous: true

must_haves:
  truths:
    - "Component showcase displays all UI elements organized by screen context"
    - "Components in showcase are interactive (buttons click, inputs accept text)"
    - "Showcase pages appear in Storybook sidebar under Showcase group"
  artifacts:
    - path: "apps/web/src/stories/showcase/BattleUI.mdx"
      provides: "Battle screen component showcase"
      contains: "<Canvas"
    - path: "apps/web/src/stories/showcase/CoreUI.mdx"
      provides: "Core reusable component showcase"
      contains: "<Canvas"
  key_links:
    - from: "apps/web/src/stories/showcase/*.mdx"
      to: "apps/web/src/components/ui/*.stories.tsx"
      via: "import and Canvas"
      pattern: "Canvas of="
---

<objective>
Create component showcase MDX pages in Storybook organized by screen context.

Purpose: Allow designers and developers to see all UI elements in one place, making theme comparison meaningful.
Output: Six MDX showcase pages covering all game screens, live interactive components in Storybook.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-theme-exploration/15-CONTEXT.md
@.planning/phases/15-theme-exploration/15-RESEARCH.md

# Existing stories to reference
@apps/web/src/components/ui/Button.stories.tsx
@apps/web/src/components/ui/Card.stories.tsx
@apps/web/src/components/ui/Badge.stories.tsx

# Game components to showcase
@apps/web/src/components/game/EncounterDisplay.tsx
@apps/web/src/components/game/PartyPanel.tsx
@apps/web/src/components/game/PokemonCard.tsx
@apps/web/src/components/game/Header.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Battle UI and Party UI showcase pages</name>
  <files>apps/web/src/stories/showcase/BattleUI.mdx, apps/web/src/stories/showcase/PartyUI.mdx</files>
  <action>
Create showcase directory: apps/web/src/stories/showcase/

**BattleUI.mdx:**
```mdx
import { Canvas, Meta } from '@storybook/blocks';
import * as ButtonStories from '../../components/ui/Button.stories';
import * as BadgeStories from '../../components/ui/Badge.stories';

<Meta title="Showcase/Battle UI" />

# Battle UI Components

Components used in the battle encounter screen.

## Action Buttons
Buttons for Attack, Catch, and Run actions.
<Canvas of={ButtonStories.Variants} />

## Pokemon Type Badges
Display Pokemon types during battle.
<Canvas of={BadgeStories.AllTypes} />

## Beveled Buttons (Modern Theme)
3D buttons for the modern theme direction.
<Canvas of={ButtonStories.Beveled} />
<Canvas of={ButtonStories.BeveledColors} />
```

**PartyUI.mdx:**
```mdx
import { Canvas, Meta } from '@storybook/blocks';
import * as CardStories from '../../components/ui/Card.stories';
import * as BadgeStories from '../../components/ui/Badge.stories';

<Meta title="Showcase/Party UI" />

# Party UI Components

Components for displaying and managing the player's party.

## Pokemon Cards
Cards displaying Pokemon in the party panel.
<Canvas of={CardStories.Default} />
<Canvas of={CardStories.Variants} />

## IV Grade Badges
Quality indicators for Pokemon stats.
<Canvas of={BadgeStories.Default} />

## Level Indicators
Show Pokemon level and XP progress.
```

Note: Reference existing stories where available. For game components without stories, add a placeholder section with component description.
  </action>
  <verify>Files exist, run Storybook and see "Showcase" group in sidebar with Battle UI and Party UI pages</verify>
  <done>BattleUI.mdx and PartyUI.mdx appear in Storybook sidebar under Showcase, showing interactive component canvases</done>
</task>

<task type="auto">
  <name>Task 2: Create Map UI and Social UI showcase pages</name>
  <files>apps/web/src/stories/showcase/MapUI.mdx, apps/web/src/stories/showcase/SocialUI.mdx</files>
  <action>
**MapUI.mdx:**
```mdx
import { Canvas, Meta } from '@storybook/blocks';
import * as ButtonStories from '../../components/ui/Button.stories';

<Meta title="Showcase/Map UI" />

# Map UI Components

Components for the interactive world map.

## Zone Nodes
Different zone types (towns, routes, caves, forests) with distinct styling.

## Zone Connections
Lines connecting zones with direction indicators.

## Map Controls
Zoom and center controls for map navigation.

## Travel Buttons
Navigation buttons for moving between zones.
<Canvas of={ButtonStories.Secondary} />
```

**SocialUI.mdx:**
```mdx
import { Canvas, Meta } from '@storybook/blocks';
import * as ButtonStories from '../../components/ui/Button.stories';
import * as CardStories from '../../components/ui/Card.stories';

<Meta title="Showcase/Social UI" />

# Social UI Components

Components for chat, friends, trades, and guild features.

## Chat Interface
Message bubbles, input field, tab navigation.

## Friend List
Online status indicators, friend actions.
<Canvas of={CardStories.Default} />

## Trade Interface
Pokemon selection, confirmation buttons.
<Canvas of={ButtonStories.Variants} />

## Guild Panel
Guild info, member list, buff activation.
```

For components without existing stories, include descriptive sections that explain the component's purpose and visual style.
  </action>
  <verify>Files exist, Storybook shows Map UI and Social UI under Showcase group</verify>
  <done>MapUI.mdx and SocialUI.mdx appear in Storybook with component descriptions and available canvases</done>
</task>

<task type="auto">
  <name>Task 3: Create Inventory UI and Core UI showcase pages</name>
  <files>apps/web/src/stories/showcase/InventoryUI.mdx, apps/web/src/stories/showcase/CoreUI.mdx</files>
  <action>
**InventoryUI.mdx:**
```mdx
import { Canvas, Meta } from '@storybook/blocks';
import * as ButtonStories from '../../components/ui/Button.stories';
import * as CardStories from '../../components/ui/Card.stories';

<Meta title="Showcase/Inventory UI" />

# Inventory UI Components

Components for shop, box, pokedex, and boost panels.

## Shop Panel
Item cards with prices and purchase buttons.
<Canvas of={ButtonStories.Primary} />

## Box Panel
Pokemon storage grid with selection.
<Canvas of={CardStories.Variants} />

## Pokedex Panel
Species entries with catch status.

## Boost Cards
Active buff display with countdown timers.
```

**CoreUI.mdx:**
```mdx
import { Canvas, Meta } from '@storybook/blocks';
import * as ButtonStories from '../../components/ui/Button.stories';
import * as CardStories from '../../components/ui/Card.stories';
import * as BadgeStories from '../../components/ui/Badge.stories';

<Meta title="Showcase/Core UI" />

# Core UI Components

Reusable components used throughout the application.

## Buttons
<Canvas of={ButtonStories.Variants} />
<Canvas of={ButtonStories.Sizes} />
<Canvas of={ButtonStories.Beveled} />

## Cards
<Canvas of={CardStories.Default} />
<Canvas of={CardStories.Variants} />

## Badges
<Canvas of={BadgeStories.Default} />
<Canvas of={BadgeStories.AllTypes} />

## Progress Bars
HP bars, XP bars, loading indicators.

## Tooltips
Hover information displays.
```

CoreUI is the most comprehensive - it should reference ALL available stories from the ui components.
  </action>
  <verify>Files exist, Storybook shows Inventory UI and Core UI under Showcase group</verify>
  <done>All six showcase pages exist and appear in Storybook sidebar, Core UI shows comprehensive component coverage</done>
</task>

</tasks>

<verification>
1. Six MDX files exist in apps/web/src/stories/showcase/
2. All pages appear in Storybook under "Showcase" group
3. Canvas components are interactive (can click buttons, hover cards)
4. Theme switching affects all showcased components
</verification>

<success_criteria>
- THEME-01 satisfied: Component showcase displays all UI elements in one place
- Components are organized by screen context (Battle, Party, Map, Social, Inventory, Core)
- Interactive components work (buttons click, hovers show effects)
- Theme dropdown changes component appearance across all showcase pages
</success_criteria>

<output>
After completion, create `.planning/phases/15-theme-exploration/15-03-SUMMARY.md`
</output>
